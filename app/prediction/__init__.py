"""AI prediction — a forward-looking, on-demand read for a single stock.

Step A (this): deterministic price signals (discount + trend) from real bars.
Later steps add the AI analyst (layered prompt + strategy) and the API/app.
See specs/STOCKPULSE_AI_PREDICTION_PLAN.md.
"""
