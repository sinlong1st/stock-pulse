# Copy this to `.env.local` (gitignored) and fill in to point the app at your
# backend. Leave EXPO_PUBLIC_API_BASE_URL empty to use the bundled mock data.
#
# EXPO_PUBLIC_* vars are inlined by Expo at build/start time.
EXPO_PUBLIC_API_BASE_URL=
EXPO_PUBLIC_API_TOKEN=
